local options = {
  ensure_installed = { "lua", "vim", "vimdoc", "terraform", "hcl", "python" },

  highlight = {
    enable = true,
    use_languagetree = true,
  },

  indent = { enable = true },
}

return options
